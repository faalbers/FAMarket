# FAMarket — Stock Screening System
# Python 3.14. Pinned loosely; lock exact versions once the build stabilizes.

# --- Data fetching ---
yfinance            # primary: quotes, financials, OHLCV, dividends
polygon-api-client  # symbol universe, OHLCV, reference data
fredapi             # macro series -> macro.db
finvizfinance       # on-demand symbol news (Output news action; NOT in fetch runs)
requests            # SEC EDGAR + generic HTTP
requests-oauthlib   # E*Trade OAuth1

# --- Rate limiting / resilience ---
ratelimit           # per-API request throttling
tenacity            # automatic retry on transient failures

# --- Data / analysis ---
pandas              # interchange format across all layers
numpy
scipy               # signal.find_peaks for trend detection; polyfit residuals
pandas-market-calendars  # last completed trading session

# --- UI: React frontend + FastAPI backend (python scripts/serve_ui.py) ---
fastapi             # REST/SSE API for the React UI (api/)
uvicorn             # ASGI server; scripts/serve_ui.py drives it programmatically

# --- UI: Streamlit (being replaced; retires at the end of the React migration) ---
streamlit           # local web UI (streamlit run app.py)
plotly              # interactive charts (normalized prices, heatmaps, radar)
streamlit-echarts==0.4.0  # Apache ECharts price chart (pinned: 0.7.0 breaks on current Streamlit)

# --- Reports ---
reportlab           # generate PDF reports (core/pdf.py + reporting/); pure-Python, no native deps
trafilatura         # extract clean article main-text + date from news pages (AI news reports)

# --- AI-augmented research (Topic 8 expansion; ad-hoc scripts only, not in fetch runs) ---
anthropic           # Claude API client (scripts/test_guidance_extraction.py)

# --- Config ---
python-dotenv       # load API keys from .env
truststore          # trust the OS cert store (corporate TLS interception) — see core/net.py
