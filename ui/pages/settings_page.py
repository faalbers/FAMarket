"""
Settings page (Topic 3.4). Phase 3 skeleton.

GUI over config/settings.py — both the UI and hand-edits write the same file.
Collapsible sections grouped by category (grouping designed during coding phase):
file paths, batch sizes / rate limits, indicator params, scoring weights
(category + within-category metric weights as sliders), peak-detection params.
"""

import streamlit as st

st.title("Settings")
st.info("Phase 3 — settings editor not implemented yet.")
