"""
Peak-detection calibration tool (Topic 4.2). Phase 3 skeleton.

Built into the UI (not a separate script). Auto-selects representative stocks
(highest/lowest R², most/least volatile), overlays detected peaks on price charts
one by one at Frank's click-through pace, and saves prominence/distance to
config/settings.py (PEAK_PROMINENCE, PEAK_DISTANCE).
"""

import streamlit as st

st.title("Peak Detection Calibration")
st.info("Phase 3 — calibration tool not implemented yet.")
